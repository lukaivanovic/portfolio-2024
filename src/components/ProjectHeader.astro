---
interface Props {
  name: string;
  duration: string;
  description: string;
  numeralSvg: string; // New prop for the numeral SVG
}

const { name, duration, description, numeralSvg }: Props = Astro.props;
---

<section class="py-[240px]">
  <!-- Project Number -->
  <div class="container -wide flex flex-row items-center">
    <div class="h-[1px] w-[16px] bg-[var(--color-border)] hidden lg:block">
    </div>
    <div
      class="text-[var(--color-bg)] w-[32px] h-[32px] bg-[var(--color-primary)] border-4 border-[var(--color-bg)] outline outline-1 outline-[var(--color-border)] z-10 flex items-center justify-center"
    >
      <Fragment set:html={numeralSvg} />
      <!-- Use the new prop here -->
    </div>
    <div class="h-[1px] flex-grow bg-[var(--color-border)]"></div>
  </div>

  <div class="container flex flex-col lg:flex-row pt-[96px] pb-[120px]">
    <div class="flex flex-col-reverse lg:flex-col gap-4">
      <h1 class="w-full lg:w-[420px] text-[var(--color-primary)]">
        {name}
      </h1>
      <p class="text-monospace text-xs text-[var(--color-tertiary)]">
        {duration}
      </p>
    </div>
    <div class="w-full max-w-[560px] text-[var(--color-secondary)] pt-3">
      <p set:html={description} />
    </div>
  </div>
</section>
